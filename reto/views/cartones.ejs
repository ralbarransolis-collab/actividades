<%- include('partials/head', { titulo }) %>
<%- include('partials/navbar') %>

<main class="container flex-grow-1 py-5">
    <div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center gap-3 mb-4">
        <div>
            <h1 class="h2 text-light fw-bold mb-1">Listado de cartones</h1>
        </div>

        <div class="d-flex gap-2">
            <button id="btnActualizar" class="btn btn-primary" type="button">
                Actualizar listado
            </button>
            <a href="/cartones/nuevo" class="btn btn-success">Nuevo cartón</a>
        </div>
    </div>

    <div id="mensaje" aria-live="polite"></div>

    <div id="contenedorCartones" class="row g-4"></div>
</main>

<script src="/js/cartones.js"></script>
<%- include('partials/footer') %>
